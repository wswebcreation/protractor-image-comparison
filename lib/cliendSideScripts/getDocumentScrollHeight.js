/**
 * Get the document scroll height, this means the actual height of the page from
 * the top to the bottom of the DOM
 *
 * @returns {number}
 */
export function getDocumentScrollHeight(){
	return document.documentElement.scrollHeight;
}